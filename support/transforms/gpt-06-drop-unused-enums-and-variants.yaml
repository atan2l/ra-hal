transforms:
  # Per the manual the lower bit is ignored for double buffer
  - !DeleteEnumVariants
    enum: gpt::vals::Ccr[ab]
    from: _11
  # These are just 32-bit (or 16-bit) integers
  - !Delete
    from: gpt::regs::(Gtccr|Gtcnt|Gtpr)
    type: All
  # Allowed but duplicates _00
  - !DeleteEnumVariants
    enum: gpt::vals::Odty
    from: _01
