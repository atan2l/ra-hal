transforms:
  # This is wrong
  - !DeleteEnumVariants
    enum: system::vals::Prkey
    from: _0X5A
  - !Add
    ir: !IR
      enum/system::vals::Prkey: !Enum
        bit_size: 8
        variants:
          - !EnumVariant
            name: PROTECT_KEY
            value: 0xA5
