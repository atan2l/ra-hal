Examples that demonstrate the `ra-hal` crate.

The easiest way to build is via `just`.
Simply run `just <chip_name>` followed by whatever cargo command, where `<chip_name>` is the name of the chip you'd like to work with.
E.g. `just ra4m1 build --release`
